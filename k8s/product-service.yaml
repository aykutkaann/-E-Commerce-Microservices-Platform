
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: default


spec:
  type: LoadBalancer
  selector:
    app: product-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  

  
